@model Pagila.ViewModel.RentalViewModel

@{
}

<h2>Kayýt Detay</h2>

<div>
    <h4>Kayýt detaylarý</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RentalDate)
        </dt>
        <dd>
            @(Model.RentalDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.InventoryId)
        </dt>
        <dd>
            @(Model.InventoryId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CustomerId)
        </dt>
        <dd>
            @(Model.CustomerId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ReturnDate)
        </dt>
        <dd>
            @(Model.ReturnDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.StaffId)
        </dt>
        <dd>
            @(Model.StaffId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.LastUpdate)
        </dt>
        <dd>
            @(Model.LastUpdate)
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Düzenle", "Edit", new { rentalId = Model.RentalId }) |
    @Html.ActionLink("Sil", "Delete", new { rentalId = Model.RentalId }) |
    @Html.ActionLink("Listeye Dön", "Index")
</p>