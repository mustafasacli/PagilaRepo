@model System.Collections.Generic.IEnumerable<Pagila.ViewModel.FilmActorViewModel>

@{
}

<h2>Liste</h2>

<p>
    @Html.ActionLink("Ekle", "Create")
</p>
<table class="table">
   <tr>
       <th>
           @Html.DisplayNameFor(model => model.LastUpdate)
       </th>
       <th>
           @Html.Label("", "Ýþlemler")
       </th>
   </tr>
   @foreach (var item in Model)
   {
       <tr>
           <td>
               @(item.LastUpdate)
           </td>
           <td>
               @Html.ActionLink("Detay", "Detail", new { actorId = item.ActorId, filmId = item.FilmId }) |
               @Html.ActionLink("Düzenle", "Edit", new { actorId = item.ActorId, filmId = item.FilmId }) |
               @Html.ActionLink("Sil", "Delete", new { actorId = item.ActorId, filmId = item.FilmId })
           </td>
       </tr>
   }
</table>
<div>
       <ul>
           <li>@Html.ActionLink("Anasayfaya dön", "Index", "Home")</li>
       </ul>
</div>