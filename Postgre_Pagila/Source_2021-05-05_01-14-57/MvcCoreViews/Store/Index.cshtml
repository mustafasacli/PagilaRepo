@model System.Collections.Generic.IEnumerable<Pagila.ViewModel.StoreViewModel>

@{
}

<h2>Liste</h2>

<p>
    @Html.ActionLink("Ekle", "Create")
</p>
<table class="table">
   <tr>
       <th>
           @Html.DisplayNameFor(model => model.ManagerStaffId)
       </th>
       <th>
           @Html.DisplayNameFor(model => model.AddressId)
       </th>
       <th>
           @Html.DisplayNameFor(model => model.LastUpdate)
       </th>
       <th>
           @Html.Label("", "Ýþlemler")
       </th>
   </tr>
   @foreach (var item in Model)
   {
       <tr>
           <td>
               @(item.ManagerStaffId)
           </td>
           <td>
               @(item.AddressId)
           </td>
           <td>
               @(item.LastUpdate)
           </td>
           <td>
               @Html.ActionLink("Detay", "Detail", new { storeId = item.StoreId }) |
               @Html.ActionLink("Düzenle", "Edit", new { storeId = item.StoreId }) |
               @Html.ActionLink("Sil", "Delete", new { storeId = item.StoreId })
           </td>
       </tr>
   }
</table>
<div>
       <ul>
           <li>@Html.ActionLink("Anasayfaya dön", "Index", "Home")</li>
       </ul>
</div>