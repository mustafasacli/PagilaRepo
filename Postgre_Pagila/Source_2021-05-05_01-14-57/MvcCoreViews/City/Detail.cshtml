@model Pagila.ViewModel.CityViewModel

@{
}

<h2>Kayýt Detay</h2>

<div>
    <h4>Kayýt detaylarý</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.City)
        </dt>
        <dd>
            @(Model.City)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CountryId)
        </dt>
        <dd>
            @(Model.CountryId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.LastUpdate)
        </dt>
        <dd>
            @(Model.LastUpdate)
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Düzenle", "Edit", new { cityId = Model.CityId }) |
    @Html.ActionLink("Sil", "Delete", new { cityId = Model.CityId }) |
    @Html.ActionLink("Listeye Dön", "Index")
</p>