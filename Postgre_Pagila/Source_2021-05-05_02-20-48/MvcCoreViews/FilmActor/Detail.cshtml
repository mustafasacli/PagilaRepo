@model Pagila.ViewModel.FilmActorViewModel

@{
}

<h2>Kayýt Detay</h2>

<div>
    <h4>Kayýt detaylarý</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.LastUpdate)
        </dt>
        <dd>
            @(Model.LastUpdate)
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Düzenle", "Edit", new { actorId = Model.ActorId, filmId = Model.FilmId }) |
    @Html.ActionLink("Sil", "Delete", new { actorId = Model.ActorId, filmId = Model.FilmId }) |
    @Html.ActionLink("Listeye Dön", "Index")
</p>