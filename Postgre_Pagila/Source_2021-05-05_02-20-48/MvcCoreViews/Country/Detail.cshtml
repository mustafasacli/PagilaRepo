@model Pagila.ViewModel.CountryViewModel

@{
}

<h2>Kayýt Detay</h2>

<div>
    <h4>Kayýt detaylarý</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Country)
        </dt>
        <dd>
            @(Model.Country)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.LastUpdate)
        </dt>
        <dd>
            @(Model.LastUpdate)
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Düzenle", "Edit", new { countryId = Model.CountryId }) |
    @Html.ActionLink("Sil", "Delete", new { countryId = Model.CountryId }) |
    @Html.ActionLink("Listeye Dön", "Index")
</p>