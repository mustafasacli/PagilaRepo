@model System.Collections.Generic.IEnumerable<Pagila.ViewModel.FilmCategoryViewModel>

@{
}

<h2>Liste</h2>

<p>
    @Html.ActionLink("Ekle", "Create")
</p>
<table class="table">
   <tr>
       <th>
           @Html.DisplayNameFor(model => model.LastUpdate)
       </th>
       <th>
           @Html.Label("", "Ýþlemler")
       </th>
   </tr>
   @foreach (var item in Model)
   {
       <tr>
           <td>
               @(item.LastUpdate)
           </td>
           <td>
               @Html.ActionLink("Detay", "Detail", new { filmId = item.FilmId, categoryId = item.CategoryId }) |
               @Html.ActionLink("Düzenle", "Edit", new { filmId = item.FilmId, categoryId = item.CategoryId }) |
               @Html.ActionLink("Sil", "Delete", new { filmId = item.FilmId, categoryId = item.CategoryId })
           </td>
       </tr>
   }
</table>
<div>
       <ul>
           <li>@Html.ActionLink("Anasayfaya dön", "Index", "Home")</li>
       </ul>
</div>