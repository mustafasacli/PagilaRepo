@model System.Collections.Generic.IEnumerable<Pagila.ViewModel.InventoryViewModel>

@{
}

<h2>Liste</h2>

<p>
    @Html.ActionLink("Ekle", "Create")
</p>
<table class="table">
   <tr>
       <th>
           @Html.DisplayNameFor(model => model.FilmId)
       </th>
       <th>
           @Html.DisplayNameFor(model => model.StoreId)
       </th>
       <th>
           @Html.DisplayNameFor(model => model.LastUpdate)
       </th>
       <th>
           @Html.Label("", "Ýþlemler")
       </th>
   </tr>
   @foreach (var item in Model)
   {
       <tr>
           <td>
               @(item.FilmId)
           </td>
           <td>
               @(item.StoreId)
           </td>
           <td>
               @(item.LastUpdate)
           </td>
           <td>
               @Html.ActionLink("Detay", "Detail", new { inventoryId = item.InventoryId }) |
               @Html.ActionLink("Düzenle", "Edit", new { inventoryId = item.InventoryId }) |
               @Html.ActionLink("Sil", "Delete", new { inventoryId = item.InventoryId })
           </td>
       </tr>
   }
</table>
<div>
       <ul>
           <li>@Html.ActionLink("Anasayfaya dön", "Index", "Home")</li>
       </ul>
</div>